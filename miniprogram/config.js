/**
 * 全局配置文件 (Global Configuration)
 * 
 * ==============================================================================
 * 【和风天气 (QWeather) 配置说明】
 * ==============================================================================
 * 1. 注册账号：前往和风天气开放平台 (https://id.qweather.com) 免费注册。
 * 2. 本地联调可在「开发者选项 -> 和风天气 API 联调诊断」中输入专属
 *    API Host 和 API Key。凭据仅加密保存在当前设备，不写入源码或备份。
 * 3. 正式发布仍应由受控后端签发短期 JWT 或代理请求，不要把长期私钥
 *    写进小程序客户端包。
 * 4. 开发者工具设置：
 *    - 微信开发者工具右上角「详情」->「本地设置」-> 勾选「不校验合法域名、web-view (业务域名) 及 HTTPS 证书」；
 *    - 若后续发布上线，应把和风控制台中的专属 API Host 加入 request 合法域名。
 * 
 * 💡 提示：
 * - 本文件始终保持空凭据占位，避免密钥进入可分发客户端包。
 * - 和风已在 2026 年逐步停用 devapi.qweather.com 等公共域名，请使用控制台
 *   提供的 *.qweatherapi.com 专属 Host。
 * ==============================================================================
 */

// 客户端包不能承载真实服务凭据。需要实况天气时，应通过受控后端
// 注入或代理；空值会让小程序安全降级到本地天气引擎。
function getObfuscatedKey() {
  return "";
}

module.exports = {
  // 和风天气 Web API Key（仅保留兼容字段，严禁在客户端提交真实值）
  QWEATHER_API_KEY: getObfuscatedKey(),
  getWeatherApiKey: getObfuscatedKey,

  // 正式环境的受控代理或专属接入点占位；本地联调请在开发者页面填写。
  QWEATHER_API_HOST: "",

  // 实时天气数据缓存时长 (默认 30 分钟，毫秒)，避免频繁发起网络请求消耗额度
  WEATHER_CACHE_TTL_MS: 30 * 60 * 1000,
};
